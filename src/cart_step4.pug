extends _layout
block page_title
  title 商品列表
block style
  link(type="text/css" , rel="stylesheet", href="css/vendor/swiper/swiper.css")
  link(type="text/css" , rel="stylesheet", href="css/cart.min.css")
block script
  script(type="text/javascript" src="js/vendor/swiper/swiper.min.js")
  script(type="text/javascript").
    $(document).ready(function () {
      jQuery('.quantity').each(function() {
        var spinner = jQuery(this),
          input = spinner.find('input[type="number"]'),
          btnUp = spinner.find('.quantity-up'),
          btnDown = spinner.find('.quantity-down'),
          min = input.attr('min'),
          max = input.attr('max');

        btnUp.click(function() {
          var oldValue = parseFloat(input.val());
          if (oldValue >= max) {
            var newVal = oldValue;
          } else {
            var newVal = oldValue + 1;
          }
          spinner.find("input").val(newVal);
          spinner.find("input").trigger("change");
        });

        btnDown.click(function() {
          var oldValue = parseFloat(input.val());
          if (oldValue <= min) {
            var newVal = oldValue;
          } else {
            var newVal = oldValue - 1;
          }
          spinner.find("input").val(newVal);
          spinner.find("input").trigger("change");
        });
      });
    });
block content
              
  section.projecttop
  section.cart_content.small
    .cartcenter
      h2.member_pagename 訂購完成
      h5.member_pagetext 恭喜您完成訂單!!

      .typeposition
        .typebox.typehere 
          i.typeicon.be-icon.be-icon-cartstep1
        .typebox.typehere 
          i.typeicon.be-icon.be-icon-cartstep2
        .typebox.typehere 
          i.typeicon.be-icon.be-icon-cartstep3
        .typebox.typehere 
          i.typeicon.be-icon.be-icon-cartstep4

      .member_center
        .guestarea
          h5.numberingtext 訂單編號
          h2.numbering 2018-1105-00001
          h6.guestsubtitle.no_border.center 感謝您購買我們的產品。<br>為了方便您了解訂單狀態，可透過訂單編號進行查詢，或是加入草菓農場Line@，將可第一手掌握更多新訊。
          .cart_buttonbox.type4
            a(href="")
              P
                i.buttonicon.be-icon.be-icon-copy
                | 複製訂單編號
            a(href="members_order.html")
              P
                i.buttonicon.be-icon.be-icon-buttonarrow
                | 訂單查詢
        .guestarea.noline
          .qrbox
            .qrimg: img(src="images/qrcode.png", alt="")
            h2.qrid Line@ : fs_farm
          h6.guestsubtitle.no_border.center 請掃描QR-Code，或是透過ID加入<br>好友喔，謝謝!!


    